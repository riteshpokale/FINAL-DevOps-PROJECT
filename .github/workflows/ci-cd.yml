name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Step 3: Build and push Docker image
      - name: Build and push Docker image
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/fullstack-nginx:latest

      # Step 4: Deploy to EC2
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Stop and remove old container if exists
            docker stop final-devops-project-fullstack-app || true
            docker rm final-devops-project-fullstack-app || true

            # Pull latest image
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/fullstack-nginx:latest

            # Run container with correct ports
            docker run -d \
              --name final-devops-project-fullstack-app \
              -p 8081:80 \
              -p 9090:8080 \
              --restart always \
              ${{ secrets.DOCKERHUB_USERNAME }}/fullstack-nginx:latest
